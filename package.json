{
  "name": "physics-engine",
  "version": "1.0.0",
  "description": "Physics engine with web visualization",
  "main": "src/main.js",
  "type": "module",
  "scripts": {
    "install": "node-gyp rebuild",
    "build": "node-gyp rebuild",
    "start": "electron .",
    "dev": "NODE_ENV=development electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "node-addon-api": "^7.0.0",
    "three": "^0.160.0"
  },
  "devDependencies": {
    "electron": "^35.1.3"
  }
}
